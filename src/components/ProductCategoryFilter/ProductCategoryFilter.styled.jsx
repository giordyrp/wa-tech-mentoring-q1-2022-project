import styled from 'styled-components';
import Button from '../Button';
import Card from '../Card';

export const ProductFilter = styled(Card)`
  padding: 10px 20px;
`;

export const Category = styled(Button)`
  margin-bottom: 5px;
  padding: 6px 10px;
  text-align: left;
`;